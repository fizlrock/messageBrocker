spring:
  h2:
    console.enabled: true
  application:
      name: denly
  datasource:
      generate-unique-name: false
  jpa:
      hibernate:
          ddl-auto: create-drop
      properties:
          hibernate:
              show_sql: true
              use_sql_comments: true
              format_sql: true
